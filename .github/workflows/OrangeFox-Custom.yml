name: OrangeFox - Custom
# Credits to:
# https://github.com/TeamWin
# https://gitlab.com/OrangeFox
# https://github.com/azwhikaru for Recovery Builder Template
# And all Contributors in every repositories I used
on:
  workflow_dispatch:
    inputs:
      DEVICE_TREE:
        description: 'OrangeFox Device Tree'
        required: true
        default: 'https://github.com/dpxdoss/android_recovery_realme_RMX2020'
      DEVICE_TREE_BRANCH:
        description: 'OrangeFox Device Tree Branch'
        required: true
        default: 'FOX_R12'
      DEVICE_PATH:
        description: 'Specify your Device Path'
        required: true
        default: 'device/realme/RMX2020'
      DEVICE_NAME:
        description: 'Specify your Device Codename'
        required: true
        default: 'RMX2020'
      BUILD_TARGET:
        description: 'Specify your Build Target'
        required: true
        default: 'recovery'
        type: choice
        options:
        - boot
        - recovery
        - vendorboot
jobs:
  build:
    name: Build OFR by ${{ github.actor }}
    runs-on: ubuntu-22.04
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      ORANGEFOX_PATH: /opt/fox_12.1  # ← Use absolute path OUTSIDE workspace
    permissions:
      contents: write
    steps:
    - name: Clean-up
      uses: rokibhasansagar/slimhub_actions@main
    
    - name: Swap Space
      uses: pierotofy/set-swap-space@master
      with:
        swap-size-gb: 24
      
    - name: Build Environment
      run: |
        sudo apt install aria2 -y
        git clone https://gitlab.com/OrangeFox/misc/scripts.git -b master
        cd scripts
        sudo bash setup/android_build_env.sh
      
    - name: Set-up Manifest
      run: |
        mkdir -p ~/OrangeFox_sync
        cd ~/OrangeFox_sync
        git config --global user.name "${{ github.actor }}"
        git config --global user.email "${{ github.actor_id }}+${{ github.actor }}@users.noreply.github.com"
        git clone https://gitlab.com/OrangeFox/sync.git
        cd ~/OrangeFox_sync/sync/
        ./orangefox_sync.sh --branch 12.1 --path ${{ env.ORANGEFOX_PATH }}
        
    - name: Clone Device Tree
      run: |
        cd ${{ env.ORANGEFOX_PATH }}
        git clone ${{ inputs.DEVICE_TREE }} -b ${{ inputs.DEVICE_TREE_BRANCH }} ./${{ inputs.DEVICE_PATH }}
        cd ${{ inputs.DEVICE_PATH }}
        echo "COMMIT_ID=$(git rev-parse HEAD)" >> $GITHUB_ENV
    
    - name: Apply Custom Patches
      env:
        DEVICE_PATH: ${{ inputs.DEVICE_PATH }}
        ORANGEFOX_PATH: ${{ env.ORANGEFOX_PATH }}
      run: |
        bash ${{ env.ORANGEFOX_PATH }}/${{ inputs.DEVICE_PATH }}/patches/patchsetup.sh
    
    - name: Building OrangeFox
      run: |
        cd ${{ env.ORANGEFOX_PATH }}
        echo "=== Checking device tree setup ==="
        ls -la device/realme/RMX2020/ || true
        echo ""
        echo "=== Sourcing build environment ==="
        source build/envsetup.sh 2>&1 || { echo "Build envsetup failed!"; exit 1; }
        echo ""
        echo "=== Running lunch ==="
        lunch twrp_${{ inputs.DEVICE_NAME }}-eng 2>&1 || { echo "Lunch failed!"; exit 1; }
        echo ""
        echo "=== Cleaning and building ==="
        make clean && mka adbd ${{ inputs.BUILD_TARGET }}image 2>&1 || { echo "Build failed!"; exit 1; }
    
    - name: Set Release Properties
      run: |
        echo "BUILD_DATE=$(TZ=Asia/Manila date +%Y%m%d)" >> $GITHUB_ENV
        cd ${{ env.ORANGEFOX_PATH }}
        DEVICE_TREE_URL="${{ inputs.DEVICE_TREE }}"
        if [[ "${DEVICE_TREE_URL}" == *.git ]]; then
          DEVICE_TREE_URL="${DEVICE_TREE_URL%.git}"
        fi
        echo "DEVICE_TREE_URL=${DEVICE_TREE_URL}" >> $GITHUB_ENV
    
    - name: Upload to Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          /opt/fox_12.1/out/target/product/${{ inputs.DEVICE_NAME }}/OrangeFox*.img
          /opt/fox_12.1/out/target/product/${{ inputs.DEVICE_NAME }}/OrangeFox*.zip
        name: OrangeFox Recovery for ${{ inputs.DEVICE_NAME }} // ${{ env.BUILD_DATE }}
        tag_name: ${{ github.run_id }}
        body: |
          ## OrangeFox Recovery Build - Unofficial
          Build: fox_12.1
          Device: [Device Tree/Branch](${{ env.DEVICE_TREE_URL }}/tree/${{ inputs.DEVICE_TREE_BRANCH }})
          Commit: Most recent [commit](${{ env.DEVICE_TREE_URL }}/commit/${{ env.COMMIT_ID }}) during building.
